theme: rennes-hugo-theme
_merge: deep
params:
  # RENNES THEME CUSTOM
  custom:
    search:
      active: true
      index: "ici"

  # OSUNY THEME
  search:
    active: false
  posts:
    index:
      layout: list # grid | list
      options:
        summary: true
        image: true

